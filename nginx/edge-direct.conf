# Mounted as conf.d/00-edge.conf when this nginx faces clients itself: nothing in
# front of it is trusted, so the forwarded protocol is simply the connection's own.
map $scheme $fwd_proto {
    default $scheme;
}
